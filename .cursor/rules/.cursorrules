# Flutter 应用开发规范

## 项目概述
这是一个 Flutter 视频应用项目，支持 Android、iOS、Web、Windows、macOS、Linux 多平台。项目使用 GetX 进行路由管理，Provider 进行状态管理，Dio 进行网络请求，SQLite 进行本地存储。

## 技术栈
- **框架**: Flutter (Dart 3.7.2+)
- **路由管理**: GetX 4.7.2
- **状态管理**: Provider 6.1.5
- **网络请求**: Dio 5.8.0+
- **本地存储**: SQLite3 2.7.5, SharedPreferences 2.5.3
- **UI组件库**: TDesign Flutter 0.2.4
- **视频播放**: fplayer 1.1.3, video_player 2.9.3
- **广告**: flutter_unionad 2.1.12
- **其他**: flutter_svg, webview_flutter, pull_to_refresh 等

## 代码风格规范

### 命名约定
1. **文件命名**:
   - Dart 文件使用 snake_case: `video_detail.dart`, `search_result.dart`
   - 目录名使用 snake_case: `video_detail/`, `getMiddleware/`

2. **类命名**:
   - 使用 PascalCase: `UserState`, `DioHttp`, `VideoDetail`
   - Widget 类名使用 PascalCase: `MainPage`, `HomePage`

3. **变量和函数命名**:
   - 使用 camelCase: `userInfoData`, `getSwiperPage`, `initSDK`
   - 私有变量使用下划线前缀: `_userInfoData`, `_dio`, `_selectedIndex`

4. **常量命名**:
   - 使用 lowerCamelCase 或 UPPER_SNAKE_CASE: `baseUrl`, `connectTimeout`

### 代码组织
1. **导入顺序**:
   ```dart
   // 1. Flutter/Dart SDK
   import 'package:flutter/material.dart';
   
   // 2. 第三方包
   import 'package:get/get.dart';
   import 'package:provider/provider.dart';
   
   // 3. 项目内部 - 相对路径
   import '../entity/video_detail_entity.dart';
   import '../http/request.dart';
   
   // 4. 项目内部 - 绝对路径
   import 'package:flutter_app/utils/ads.dart';
   ```

2. **文件结构**:
   ```dart
   // 导入语句
   // ...
   
   // 类定义
   class MyClass {
     // 静态变量
     // 实例变量
     // 构造函数
     // 方法
     // 私有方法
   }
   ```

### 注释规范
1. **文档注释**: 使用 `///` 为公共 API 添加文档注释
   ```dart
   /// 异步获取轮播图页面数据
   /// 
   /// 该方法通过POST请求从服务器获取轮播图页面数据
   static Future<SwiperEntity> getSwiperPage(Map<String, dynamic>? data) async {
   ```

2. **行内注释**: 使用 `//` 解释复杂逻辑
   ```dart
   // 当切换到"我的"页面时，调用页面的刷新方法
   if (index == 4) {
     _myPageKey.currentState?.init();
   }
   ```

3. **中文注释**: 项目使用中文注释，保持注释清晰易懂

## 架构模式

### 目录结构
```
lib/
├── api/              # API 接口定义
├── components/       # 可复用组件
├── core/             # 核心功能模块
├── db/               # 数据库相关
│   ├── entity/       # 数据库实体
│   └── manager/      # 数据库管理器
├── entity/           # 业务实体类
├── generated/        # 生成的代码（如 JSON 序列化）
├── getMiddleware/     # GetX 中间件
├── http/             # HTTP 请求相关
├── models/           # 数据模型
├── providers/        # Provider 状态管理
├── services/         # 业务服务
├── style/            # 样式定义
├── utils/            # 工具类
│   └── store/        # 状态存储
├── views/            # 页面视图
└── widgets/          # 自定义 Widget
```

### 状态管理
1. **全局状态**: 使用 Provider 管理
   - `UserState`: 用户状态
   - `ThemeChangeEvent`: 主题状态
   - `AppState`: 应用状态

2. **页面状态**: 使用 StatefulWidget 的 State 管理

3. **路由管理**: 使用 GetX 进行路由导航
   ```dart
   Get.toNamed('/video_detail');
   Get.back();
   ```

### 网络请求
1. **统一使用 DioHttp 类**: 所有 API 请求通过 `Api` 类的静态方法
2. **拦截器**: 
   - `TokenInterceptors`: Token 管理
   - `CacheInterceptor`: 缓存管理
   - `ErrorInterceptor`: 错误处理
   - `ResponseInterceptor`: 响应处理
   - `PrintLogInterceptor`: 日志打印

3. **API 定义**: 在 `lib/api/api.dart` 中统一管理所有接口

### 数据库
1. **使用 SQLite3**: 通过 `DBManager` 统一管理
2. **实体类**: 放在 `lib/db/entity/` 目录
3. **数据库助手**: 每个实体对应一个 Helper 类

## 最佳实践

### Widget 开发
1. **优先使用 StatelessWidget**: 除非需要状态管理，否则使用 StatelessWidget
2. **提取可复用组件**: 将重复的 UI 代码提取到 `components/` 目录
3. **使用 const 构造函数**: 尽可能使用 const 构造函数优化性能
   ```dart
   const Home(),
   const VideoFilter(),
   ```

4. **避免不必要的重建**: 使用 `IndexedStack` 保持页面状态
   ```dart
   IndexedStack(
     index: _selectedIndex,
     children: pages,
   )
   ```

### 异步处理
1. **使用 async/await**: 优先使用 async/await 而非 Future.then
2. **错误处理**: 使用 try-catch 处理异步错误
   ```dart
   try {
     await Api.getAdsList({});
   } catch (e) {
     debugPrint('获取广告失败: $e');
   }
   ```

3. **并发执行**: 使用 `Future.wait` 并发执行多个异步操作
   ```dart
   await Future.wait([
     DBManager.init(),
     initPlatformState(),
   ]);
   ```

### 资源管理
1. **图片资源**: 放在 `assets/images/` 目录
2. **SVG 图标**: 使用 `flutter_svg` 加载 SVG 文件
   ```dart
   SvgPicture.asset(
     'assets/images/sy_ic01h.svg',
     width: 25,
     height: 25,
   )
   ```

3. **资源声明**: 在 `pubspec.yaml` 中声明所有资源

### 错误处理
1. **网络错误**: 通过 `ErrorInterceptor` 统一处理
2. **UI 错误**: 显示友好的错误提示，使用 `TDToast` 或 `fluttertoast`
3. **日志记录**: 使用 `debugPrint` 而非 `print`

### 性能优化
1. **懒加载**: 使用 `ListView.builder` 而非 `ListView` 处理长列表
2. **图片优化**: 使用适当尺寸的图片，考虑使用缓存
3. **避免过度重建**: 使用 `const` 和 `GlobalKey` 优化 Widget 重建

## 代码质量

### Lint 规则
- 遵循 `analysis_options.yaml` 中的 lint 规则
- 使用 `flutter_lints` 包提供的推荐规则

### 代码审查要点
1. **可读性**: 代码清晰易懂，注释充分
2. **可维护性**: 模块化设计，职责单一
3. **性能**: 避免不必要的计算和重建
4. **安全性**: 正确处理用户输入和敏感数据

## 开发工作流

### 添加新功能
1. 在 `lib/entity/` 创建实体类
2. 在 `lib/api/api.dart` 添加 API 方法
3. 在 `lib/views/` 创建页面（如需要）
4. 在 `lib/main.dart` 注册路由（如需要）
5. 更新相关状态管理（如需要）

### 添加新页面
1. 在 `lib/views/` 创建页面目录和文件
2. 在 `lib/main.dart` 的 `getPages` 中添加路由
3. 使用 GetX 导航: `Get.toNamed('/page_name')`

### 添加新组件
1. 在 `lib/components/` 创建组件文件
2. 确保组件可复用和独立
3. 使用适当的参数和回调

## 注意事项

1. **多平台兼容**: 确保代码在所有目标平台上正常工作
2. **广告集成**: 广告相关代码在 `lib/ads/` 和 `lib/utils/ads.dart`
3. **主题支持**: 支持明暗主题切换，使用 `ThemeChangeEvent`
4. **权限处理**: 使用 `permission_handler` 处理运行时权限
5. **设备信息**: 使用 `device_info_plus` 获取设备信息
6. **视频播放**: 使用 `fplayer` 或 `video_player` 进行视频播放
7. **网络状态**: 使用 `connectivity_plus` 监听网络状态

## 代码生成

- JSON 序列化代码在 `lib/generated/json/` 目录
- 使用代码生成工具生成实体类的序列化代码

## 测试

- 测试文件放在 `test/` 目录
- 使用 `flutter_test` 进行单元测试和 Widget 测试

## 响应式设计

- 考虑不同屏幕尺寸
- 使用 `MediaQuery` 获取屏幕信息
- 使用 `LayoutBuilder` 进行响应式布局

